2020-02-18

NEW:

-  Updated Desktop to 4.24.1

FIXED:

- Fixed display of non UTF-8 sidecar subtitles
- Fixed long press of backspace causing loss of focus when closing modals
- Fixed an issue that caused search to never be performed when using the pointer
- Fixed an issue that could cause the sidebar to become inaccessible after stopping playback from a controller app
- Fixed loss of progress when VOD is stopped during initial ad after resuming
- Fixed missing focus in `Plex is not reachable` page
- Fixed an issue that caused sidebar items to display an infinite spinner when the app is offline
- Fixed possible error on user switcher when plex.tv authentication is unavailable
